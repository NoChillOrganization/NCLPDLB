Check for any existing ML model.

If present:

* Refactor into a transformer-based policy/value model

If not:

* Implement from scratch

Requirements:

* PyTorch
* CPU-friendly (Windows laptop)
* Input: sequence of states
* Output:

  * policy logits
  * value

Keep it lightweight and modular.
