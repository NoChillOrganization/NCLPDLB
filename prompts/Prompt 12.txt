You are working inside an existing code repository.

Your task is to **analyze, refactor, optimize, and extend this repo** into a complete:

👉 Local-only Pokémon Showdown AI training system
👉 Running on Windows
👉 Controlled and monitored through a browser dashboard

---

## 🔒 STRICT REQUIREMENTS

* MUST run on Windows 10/11
* MUST run locally only (no cloud training)
* MUST be controllable via browser (FastAPI + HTML UI)
* MUST integrate with a local Pokémon Showdown server
* MUST reuse and improve existing repo code (DO NOT rewrite blindly)

---

## 📂 ADDITIONAL INPUT DATA (CRITICAL)

There is a file in the repo:

**"batch fetch and extract key learning data for Pokemon Draft Leagues, Smogon Formats, and VGC Formats"**

Use this file as a **data ingestion blueprint**.

Your job:

* Extract relevant competitive knowledge sources
* Identify:

  * battle strategies
  * team building patterns
  * damage calculation tools
  * replay datasets
* Design a system to:

  * ingest replay data (if available)
  * structure it for training
  * optionally improve AI decision-making

DO NOT just summarize it—**integrate it into the system design**.

---

## 🧠 PHASE 1 — REPO ANALYSIS

Analyze the entire repository.

Output:

1. Current architecture
2. Existing components:

   * Showdown connection
   * battle logic
   * ML/training
3. What is missing for a full AI system
4. What should be refactored vs reused

DO NOT modify code yet.

---

## 🧠 PHASE 2 — SYSTEM DESIGN (BASED ON EXISTING CODE)

Design a complete system using the repo as a base:

### Required Components:

* Showdown WebSocket client (2 accounts)
* Battle parser (structured state)
* Transformer model (policy + value)
* MCTS decision engine
* Self-play system (AccountA vs AccountB)
* Replay buffer + training loop
* FastAPI dashboard backend
* HTML dashboard frontend
* Optional replay ingestion system (from dataset links)

Ensure:

* Everything is modular
* Windows-compatible
* CPU-friendly

---

## ⚔️ PHASE 3 — IMPLEMENTATION + REFACTOR

Now implement and refactor:

### 1. Showdown Client

* Default:
  ws://localhost:8000/showdown/websocket
* Support 2 accounts
* Stable on Windows (threading, not asyncio-heavy)

---

### 2. Battle Parser

* Extract:
  moves, HP, turn number
* Return structured state

---

### 3. Transformer Model

* PyTorch
* Input: sequence of states (last 4–8 turns)
* Output:

  * policy logits
  * value

---

### 4. MCTS Integration

* Node + UCB
* Expansion using model
* Backpropagation
* 25–50 simulations per move
* Must integrate into decision pipeline

---

### 5. Self-Play System

* AccountA vs AccountB (direct challenge)
* Continuous loop
* Store:
  state, action, reward

---

### 6. Training System

* Replay buffer (JSONL + memory)
* Batch sampling
* Loss:
  cross entropy + MSE
* Save model:
  models/latest.pt

---

### 7. Learning Data Integration (IMPORTANT)

Using the provided learning-data file:

* Add optional module:
  data/ingestion.py

Capabilities:

* Load replay datasets (if available)
* Parse into training format
* Optionally augment self-play data

Focus on:

* improving training quality
* not just collecting links

---

### 8. Dashboard Backend (FastAPI)

Global STATE:

* games
* wins
* losses
* winrate
* status
* mcts_sims

Endpoints:
GET /stats
POST /start
POST /stop
POST /config

---

### 9. Dashboard Frontend (HTML)

Single file UI:

* Dark theme
* Chart.js graph
* Auto-refresh stats
* Controls:
  start/stop
  MCTS slider
* Embed:
  http://localhost:8000 (Showdown UI)

---

### 10. Main Runner

* Use threading (Windows safe)
* Start:

  * self-play loop
  * training loop
* Controlled by dashboard STATE

---

## ⚡ PHASE 4 — OPTIMIZATION

Refactor and optimize:

* Remove duplicate code
* Improve performance:

  * MCTS speed
  * training loop efficiency
* Reduce memory usage
* Ensure thread safety (Windows)

---

## 🧪 PHASE 5 — VALIDATION

Ensure:

* System runs locally
* No broken imports
* No unused code
* Clean structure

---

## 🖥️ FINAL OUTPUT

Provide:

1. Updated/refactored code
2. New modules added
3. Clear Windows instructions:

* Start Showdown
* Start FastAPI
* Open dashboard
* Start training

---

## 🎯 FINAL GOAL

A **fully working, local, browser-controlled AI training system** that:

* Uses self-play + MCTS + transformer
* Can optionally leverage real competitive data
* Is stable and optimized for a Windows laptop

---

Focus on:

* correctness
* integration
* stability
* performance

NOT just generating code—**build a cohesive system**
