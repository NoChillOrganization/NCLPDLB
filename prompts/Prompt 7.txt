Find existing training code.

Refactor into:

* Replay buffer system
* Batch sampling
* Training loop:

  * cross entropy (policy)
  * MSE (value)

Optimize for:

* CPU usage
* low memory
* Windows stability

Save model to:
models/latest.pt

Avoid rewriting unless necessary.
