# Pokemon Draft League Bot - Python Dependencies
# Cross-platform compatible (Windows, macOS, Linux)

# Build tooling (pinned to avoid CVE-2025-47273 path traversal in PackageIndex)
setuptools>=83.0.0

# Discord Bot
discord.py>=2.7.1

# Google Sheets Integration
gspread>=6.2.1
google-auth>=2.56.0
google-auth-oauthlib>=1.4.0

# Data Validation
pydantic>=2.13.4
pydantic-settings>=2.14.2

# HTTP Client (async, cross-platform)
aiohttp>=3.14.1,<3.15
httpx>=0.28.1
requests>=2.34.2  # transitive dep of gspread, but also imported directly in src/data/sheets.py; floor covers CVE-2024-47081 (.netrc leak) + CVE-2026-25645 (insecure temp file)

# Data Scraping (Smogon)
beautifulsoup4>=4.15.0
lxml>=6.1.1

# Database
aiosqlite>=0.22.1
asyncpg>=0.31.0  # platform Postgres pool (src/platform/store/db.py)

# Path Handling (cross-platform, stdlib)
# pathlib is built-in to Python 3.4+

# Testing
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
pytest-mock>=3.15.1
pytest-timeout>=0.14.0
locust>=2.45.0  # Load testing (tests/performance/locustfile.py)

# Code Quality
ruff>=0.15.21

# Machine Learning — data pipeline
numpy>=2.4.6,<2.5.0  # compatible with Python 3.11+
scikit-learn>=1.9.0
pandas>=3.0.3

# Machine Learning — deep learning + RL
# CI installs torch via: pip install torch --index-url https://download.pytorch.org/whl/cpu
# Local install: pip install torch  (or see https://pytorch.org/get-started/locally/ for CUDA)
torch>=2.10.0
stable-baselines3>=2.8.0   # PPO/DQN for battle policy RL
gymnasium>=1.3.0            # required by poke-env==0.15.0; also imported directly (guarded) in battle_env.py/pretrain.py/train_policy.py
tensorboard>=2.21.0         # training metrics visualisation
# imitation is NOT listed here: poke-env==0.15.0 requires gymnasium>=1.3.0 while
# imitation>=1.0.0 requires gymnasium~=0.29 — they cannot coexist in one environment.
# For local BC pre-training use a separate venv:
#   pip install "imitation>=1.0.0" "gymnasium~=0.29" "stable-baselines3>=1.7"
# (BC pre-training in CI is skipped when no replay JSON files are present.)

# Pokemon Showdown battle environment (RL training)
# poke-env connects to a local Showdown server for self-play training;
# the Discord /spar command instead targets the public wss://sim3.psim.us
# (see src/ml/showdown_modes.py) and needs a real registered bot account.
poke-env==0.15.0            # pinned: code uses the 0.13+ module layout (poke_env.battle, Singles/DoublesEnv); 0.12.x will break imports
websockets>=16.0       # required by poke-env for Showdown WebSocket communication
fastapi>=0.139.0       # ML training API server (src/ml/api.py, run_training.py)
uvicorn>=0.51.0        # ASGI server for FastAPI ML API

# Packaging (build standalone .exe)
pyinstaller>=6.21.0       # build: pyinstaller src/bot/NCLPDLB.spec
