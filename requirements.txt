# Pokemon Draft League Bot - Python Dependencies
# Cross-platform compatible (Windows, macOS, Linux)

# Discord Bot
discord.py>=2.7.1

# Google Sheets Integration
gspread>=6.0.0
google-auth>=2.27.0
google-auth-oauthlib>=1.4.0

# Data Validation
pydantic>=2.6.0
pydantic-settings>=2.14.1

# HTTP Client (async, cross-platform)
aiohttp>=3.9.3
httpx>=0.28.1

# Data Scraping (Smogon)
beautifulsoup4>=4.14.3
lxml>=6.1.0

# Database
aiosqlite>=0.20.0

# Environment Config
python-dotenv>=1.2.2

# Path Handling (cross-platform, stdlib)
# pathlib is built-in to Python 3.4+

# Scheduling / Async Tasks
apscheduler>=3.11.2

# Date/Time
python-dateutil>=2.9.0

# Testing
pytest>=9.0.3
pytest-asyncio>=1.3.0
pytest-cov>=4.1.0
pytest-mock>=3.15.1
respx>=0.21.0  # Mock httpx requests
aioresponses>=0.7.8  # Mock aiohttp requests

# Code Quality
ruff>=0.15.12
mypy>=1.9.0

# Machine Learning — data pipeline
numpy>=1.26.0
scikit-learn>=1.8.0
pandas>=3.0.2

# Machine Learning — deep learning + RL
# CI installs torch via: pip install torch --index-url https://download.pytorch.org/whl/cpu
# Local install: pip install torch  (or see https://pytorch.org/get-started/locally/ for CUDA)
# torch>=2.2.0  (uncomment only if you want pip to manage torch for you)
stable-baselines3>=2.8.0   # PPO/DQN for battle policy RL
tensorboard>=2.20.0         # training metrics visualisation
imitation>=1.0.0            # behavioral cloning pre-training (BC pre-trainer)
# imitation is NOT listed here — it requires gymnasium~=0.29 which conflicts with
# poke-env's gymnasium>=1.0.0 requirement. Install separately in a dedicated venv:
#   pip install "imitation>=0.7.0" "gymnasium~=0.29"

# Pokemon Showdown battle environment (RL training)
# poke-env connects to a local Showdown server for self-play
poke-env==0.12.1            # pinned: 0.13+ has breaking API changes
websockets>=16.0       # required by poke-env for Showdown WebSocket communication
