# Pokemon Draft League Bot - Python Dependencies
# Cross-platform compatible (Windows, macOS, Linux)

# Build tooling (pinned to avoid CVE-2025-47273 path traversal in PackageIndex)
setuptools>=82.0.1

# Discord Bot
discord.py>=2.7.1

# Google Sheets Integration
gspread>=6.2.1
google-auth>=2.53.0
google-auth-oauthlib>=1.4.0

# Data Validation
pydantic>=2.13.4
pydantic-settings>=2.14.1

# HTTP Client (async, cross-platform)
aiohttp>=3.13.5,<3.14  # aioresponses 0.7.8 incompatible with 3.14+ (stream_writer kwarg)
httpx>=0.28.1

# Data Scraping (Smogon)
beautifulsoup4>=4.14.3
lxml>=6.1.1

# Database
aiosqlite>=0.22.1

# Path Handling (cross-platform, stdlib)
# pathlib is built-in to Python 3.4+

# Date/Time
python-dateutil>=2.9.0.post0

# Testing
pytest>=9.0.3
pytest-asyncio>=1.3.0
pytest-cov>=7.1.0
pytest-mock>=3.15.1
respx>=0.23.1  # Mock httpx requests
aioresponses>=0.7.8  # Mock aiohttp requests
locust>=2.44.0  # Load testing (tests/performance/locustfile.py)

# Code Quality
ruff>=0.15.14
mypy>=2.1.0

# Machine Learning — data pipeline
numpy>=2.4.6
scikit-learn>=1.9.0
pandas>=3.0.3

# Machine Learning — deep learning + RL
# CI installs torch via: pip install torch --index-url https://download.pytorch.org/whl/cpu
# Local install: pip install torch  (or see https://pytorch.org/get-started/locally/ for CUDA)
torch>=2.8.0
stable-baselines3>=2.8.0   # PPO/DQN for battle policy RL
playwright>=1.60.0          # Headless browser automation; run `playwright install chromium` after pip install
tensorboard>=2.20.0         # training metrics visualisation
# imitation is NOT listed here: poke-env==0.12.1 requires gymnasium>=1.0.0 while
# imitation>=1.0.0 requires gymnasium~=0.29 — they cannot coexist in one environment.
# For local BC pre-training use a separate venv:
#   pip install "imitation>=1.0.0" "gymnasium~=0.29" "stable-baselines3>=1.7"
# (BC pre-training in CI is skipped when no replay JSON files are present.)

# Pokemon Showdown battle environment (RL training)
# poke-env connects to a local Showdown server for self-play
poke-env==0.12.1            # pinned: 0.13+ has breaking API changes
websockets>=16.0       # required by poke-env for Showdown WebSocket communication
fastapi>=0.136.3       # ML training API server (src/ml/api.py, run_training.py)
uvicorn>=0.48.0        # ASGI server for FastAPI ML API
