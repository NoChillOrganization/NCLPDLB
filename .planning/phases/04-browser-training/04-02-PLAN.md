---
phase: 04-browser-training
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - tests/unit/test_browser_trainer.py
autonomous: true
requirements:
  - BRWS-01
  - BRWS-02
  - BRWS-03

must_haves:
  truths:
    - "Unit tests cover the credential routing fix (MODE_BROWSER raises ValueError without env vars)"
    - "Unit tests cover build_observation_from_dom() returning a zero float32 vector of shape (48,)"
    - "Unit tests cover the DEFAULT_RESULTS_DIR consistency check (browser_trainer uses data/ml/results)"
    - "Unit tests run without Playwright installed and without a live browser or network"
    - "All new tests pass under the existing pytest configuration"
  artifacts:
    - path: "tests/unit/test_browser_trainer.py"
      provides: "Unit test coverage for browser_trainer.py fixed functions"
      contains: "TestAccountConfigsForModeBrowser"
  key_links:
    - from: "tests/unit/test_browser_trainer.py"
      to: "src/ml/showdown_modes.account_configs_for_mode"
      via: "patch('src.ml.showdown_modes.os.environ.get')"
      pattern: "TestAccountConfigsForModeBrowser"
    - from: "tests/unit/test_browser_trainer.py"
      to: "src/ml/browser_trainer.build_observation_from_dom"
      via: "MagicMock() Playwright page stub"
      pattern: "MagicMock"
---

<objective>
Write unit tests for the three bugs fixed in Plan 01 and for the core pure-function behavior of browser_trainer.py. Tests must run entirely without Playwright, a real browser, or network access.

Purpose: The fixes in Plan 01 are untested. This plan provides the automated verification layer that makes BRWS-01/02/03 mechanically verifiable on every CI run.

Output: tests/unit/test_browser_trainer.py with test classes covering: credential routing, DEFAULT_RESULTS_DIR consistency, build_observation_from_dom() with a MagicMock page, and _ReplayEnv step/reset behavior.
</objective>

<execution_context>
@/home/vboxuser/Documents/No Chill Draft League Bot/NCLPDLB/.planning/phases/04-browser-training/04-PATTERNS.md
@/home/vboxuser/Documents/No Chill Draft League Bot/NCLPDLB/.planning/phases/04-browser-training/04-01-SUMMARY.md
</execution_context>

<context>
@/home/vboxuser/Documents/No Chill Draft League Bot/NCLPDLB/.planning/ROADMAP.md

<interfaces>
<!-- Contracts implemented in Plan 01 that these tests verify. -->

From src/ml/showdown_modes.py (after Plan 01 fix):
```python
def account_configs_for_mode(mode: str) -> tuple:
    # MODE_BROWSER now handled same as MODE_SHOWDOWN
    # Raises ValueError when SHOWDOWN_TRAIN_USER1/PASS1/USER2/PASS2 env vars absent
    # Returns (AccountConfiguration, AccountConfiguration) when all 4 vars present
```

From src/ml/browser_trainer.py (after Plan 01 fixes):
```python
def build_observation_from_dom(page: Any) -> np.ndarray:
    # Returns float32 ndarray of shape (OBS_DIM=48,)
    # Falls back to zeros if DOM read fails

# _ReplayEnv class added in Plan 01 — test its step() / reset() contract:
class _ReplayEnv(gym.Env):
    def __init__(self, transitions: list) -> None: ...
    def reset(self, **kwargs): ...  # returns (zeros_obs, {})
    def step(self, action): ...     # pops from transitions; returns (obs, reward, done, False, {})
```

From src/ml/battle_env.py:
```python
OBS_DIM = 48
```

Test file analog (tests/unit/test_train_policy.py lines 1-60):
```python
from __future__ import annotations
from unittest.mock import MagicMock, patch
import pytest
from src.ml.battle_env import OBS_DIM
# class TestXxx groups tests per function; each method tests one behavior
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test_browser_trainer.py covering credential routing, results dir, build_observation_from_dom, and _ReplayEnv</name>
  <files>tests/unit/test_browser_trainer.py</files>
  <read_first>
    - tests/unit/test_train_policy.py — lines 1-80 (file structure, import pattern, class grouping pattern, MagicMock usage)
    - tests/unit/test_showdown_player.py — scan for any existing Playwright mock pattern (reference only)
    - src/ml/browser_trainer.py — full file (to understand _ReplayEnv internals and build_observation_from_dom behavior)
    - .planning/phases/04-browser-training/04-PATTERNS.md — "test_browser_trainer.py" section for mock patterns
  </read_first>
  <behavior>
    - TestAccountConfigsForModeBrowser.test_raises_without_env_vars: call account_configs_for_mode(MODE_BROWSER) with no env vars set; assert ValueError raised; assert message contains "SHOWDOWN_TRAIN_USER1"
    - TestAccountConfigsForModeBrowser.test_raises_partial_env_vars: set only USER1/PASS1 (not USER2/PASS2); assert ValueError raised
    - TestAccountConfigsForModeBrowser.test_returns_account_configurations_when_all_vars_set: patch os.environ with all 4 vars; assert result is a 2-tuple of non-None objects; assert result[0].username == "user1" (or whatever poke-env AccountConfiguration exposes — use a try/except skip if poke-env not installed)
    - TestDefaultResultsDir.test_browser_trainer_uses_same_results_dir_as_train_policy: import DEFAULT_RESULTS_DIR from both modules; assert they are equal (string comparison after normalizing with Path)
    - TestBuildObservationFromDom.test_returns_zero_vector_on_empty_page: create MagicMock page with locator(...).count() returning 0; call build_observation_from_dom(page); assert shape == (OBS_DIM,); assert dtype == float32; assert all zeros
    - TestBuildObservationFromDom.test_returns_float32_array: same mock; assert obs.dtype.name == "float32"
    - TestBuildObservationFromDom.test_does_not_raise_on_dom_exception: mock page.locator raising Exception; assert build_observation_from_dom does not raise; assert result shape == (OBS_DIM,)
    - TestReplayEnv.test_step_returns_stored_transition: create _ReplayEnv with one transition [(obs_arr, 1.0, True)]; call reset(); call step(0); assert returned obs matches obs_arr; assert reward == 1.0; assert terminated == True
    - TestReplayEnv.test_step_returns_zeros_when_exhausted: create _ReplayEnv with empty list; call reset(); call step(0); assert terminated == True; assert reward == 0.0
    - TestReplayEnv.test_reset_replays_from_start: create _ReplayEnv with 2 transitions; call step twice to exhaust; call reset(); call step(0) again; assert first transition replayed
  </behavior>
  <action>
Create tests/unit/test_browser_trainer.py following the structure below.

File structure:
1. Module docstring explaining scope (pure-function tests only; no browser; no network)
2. `from __future__ import annotations`
3. Standard imports: unittest.mock.MagicMock, patch; numpy as np; pytest
4. SB3 availability guard (same pattern as test_train_policy.py):
   ```
   try:
       from stable_baselines3 import PPO
       SB3_OK = True
   except ImportError:
       SB3_OK = False
   ```
5. poke-env availability guard:
   ```
   try:
       from poke_env.ps_client.account_configuration import AccountConfiguration
       POKE_ENV_OK = True
   except ImportError:
       POKE_ENV_OK = False
   ```
6. Import OBS_DIM from src.ml.battle_env

Test classes (one per function under test):

TestAccountConfigsForModeBrowser:
- Import `account_configs_for_mode, MODE_BROWSER` from src.ml.showdown_modes inside each test method (avoids module-level import failure if poke-env absent).
- For tests that need poke-env AccountConfiguration, decorate with `@pytest.mark.skipif(not POKE_ENV_OK, reason="poke-env not installed")`.
- For the "raises without env vars" test: use `patch.dict(os.environ, {}, clear=True)` scoped to clear the four SHOWDOWN_TRAIN_* vars, then assert pytest.raises(ValueError, match="SHOWDOWN_TRAIN_USER1").
- For the "all vars set" test: use `patch.dict(os.environ, {"SHOWDOWN_TRAIN_USER1": "u1", "SHOWDOWN_TRAIN_PASS1": "p1", "SHOWDOWN_TRAIN_USER2": "u2", "SHOWDOWN_TRAIN_PASS2": "p2"})`.

TestDefaultResultsDir:
- Import both DEFAULT_RESULTS_DIR values; use Path() normalization; assert Path(browser_results_dir) == Path(train_policy_results_dir).
- This test has no external deps and never needs a skip marker.

TestBuildObservationFromDom:
- Import build_observation_from_dom from src.ml.browser_trainer inside each test (lazy import avoids playwright ImportError at collection time).
- Construct mock_page = MagicMock(); mock_page.locator.return_value.count.return_value = 0 for the empty-page case.
- For the exception case: mock_page.locator.side_effect = Exception("DOM exploded"); assert no raise and shape correct.

TestReplayEnv:
- Import _ReplayEnv lazily inside each test: `from src.ml.browser_trainer import train_browser; import inspect; ...` — actually, _ReplayEnv is defined inside train_browser() so it cannot be imported directly. Instead, write a minimal inline copy of the class contract in the test to verify the spec, OR test through a thin helper. Preferred: skip _ReplayEnv direct unit tests and instead verify the behavior via integration with a stub policy.
- Alternative (simpler): extract the _ReplayEnv class body to a module-level private class `_ReplayEnv` in browser_trainer.py rather than defining it inside train_browser(). This makes it directly importable. Update the action in Plan 01 accordingly: the class definition should be at module level (after _FakeEnv, around line 280), not inside train_browser(). The lambda in the learn() call becomes `lambda t=all_transitions: _ReplayEnv(t)` unchanged.
- With _ReplayEnv at module level, import it: `from src.ml.browser_trainer import _ReplayEnv`; test step/reset contract directly.
- For TestReplayEnv tests: create np.zeros arrays as obs, build transition tuples, instantiate _ReplayEnv, call reset() and step() and verify the returned values match the spec in the behavior block above.

All tests must pass when run with: `.venv/bin/python -m pytest tests/unit/test_browser_trainer.py -v`
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest /home/vboxuser/Documents/No\ Chill\ Draft\ League\ Bot/NCLPDLB/tests/unit/test_browser_trainer.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File tests/unit/test_browser_trainer.py exists
    - pytest collects at least 10 test items from the file
    - All collected tests pass (0 failures, 0 errors)
    - No test requires a real Playwright browser or network connection
    - .venv/bin/python -m ruff check tests/unit/test_browser_trainer.py exits 0
    - TestAccountConfigsForModeBrowser.test_raises_without_env_vars PASSES (confirms credential routing bug is fixed)
    - TestDefaultResultsDir test PASSES (confirms results dir alignment)
    - TestBuildObservationFromDom.test_returns_zero_vector_on_empty_page PASSES with obs.shape == (48,)
    - TestReplayEnv.test_step_returns_stored_transition PASSES (confirms _ReplayEnv contract)
  </acceptance_criteria>
  <done>test_browser_trainer.py created with all test classes; pytest runs green; ruff passes; _ReplayEnv moved to module level in browser_trainer.py if needed for direct import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env vars | Tests that simulate credential presence use patch.dict(os.environ) — no real credentials used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | test env var patches | accept | Tests use dummy strings ("u1", "p1"); no real credentials; patch.dict scope-limits exposure |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new installs in this plan; playwright was gated in Plan 01 |
</threat_model>

<verification>
After task completes:

1. Full test run: `.venv/bin/python -m pytest tests/unit/test_browser_trainer.py -v`
2. Ruff clean: `.venv/bin/python -m ruff check tests/unit/test_browser_trainer.py`
3. No regression: `.venv/bin/python -m pytest tests/unit/ -q --tb=no 2>&1 | tail -5`
</verification>

<success_criteria>
- tests/unit/test_browser_trainer.py exists with at least 10 test methods
- All tests pass without a live browser or network
- TestAccountConfigsForModeBrowser confirms MODE_BROWSER credential routing works
- TestDefaultResultsDir confirms both modules use data/ml/results
- TestReplayEnv confirms _ReplayEnv step/reset contract
- No existing unit tests broken
</success_criteria>

<output>
Create `.planning/phases/04-browser-training/04-02-SUMMARY.md` when done.
</output>
