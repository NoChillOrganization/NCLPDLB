---
context: phase
phase: 04-browser-training
task: 2
total_tasks: 2
status: complete
last_updated: 2026-05-20T03:50:13+00:00
---

# Phase 04 Complete — Clean Handoff

_No blocking constraints. Phase fully executed and committed._

<current_state>
Phase 04 (Browser Training) is DONE. Both plans executed and committed.
Next: plan and execute Phase 05 (MCTSPlayer + Transformer Training).
No uncommitted changes. All tests green.
</current_state>

<completed_work>

- Task 1 (Plan 04-01) `548667a`: Fixed 3 bugs blocking browser training
  - `showdown_modes.py`: `account_configs_for_mode()` now includes `MODE_BROWSER` in condition — was `if mode == MODE_SHOWDOWN:`, now `if mode in (MODE_SHOWDOWN, MODE_BROWSER):`
  - `browser_trainer.py`: Corrected `DEFAULT_RESULTS_DIR` from `"src/ml/models/results"` → `"data/ml/results"`
  - `browser_trainer.py`: Added `_ReplayEnv` class (module level) + `all_transitions` accumulation + `policy.learn()` calls at each checkpoint and end-of-training
  - `requirements.txt`: Added `playwright>=1.52.0`

- Task 2 (Plan 04-02) `5d7dae9`: 14 unit tests in `tests/unit/test_browser_trainer.py`
  - `TestAccountConfigsForModeBrowser` (4): credential routing, partial vars, error message, full vars
  - `TestDefaultResultsDir` (1): path alignment between browser_trainer and train_policy
  - `TestBuildObservationFromDom` (4): zero vector, float32, OBS_DIM, exception safety
  - `TestReplayEnv` (5): step contract, empty transitions, reset replay, reset return, space attrs
</completed_work>

<remaining_work>
Nothing in Phase 04. Next phase not yet planned.

Phase 05: MCTSPlayer + Transformer Training (requirements MCTS-01, MCTS-02, MCTS-03)
- MCTSPlayer unit + integration tests with meaningful coverage
- Training pipeline accepts MCTSPlayer as self-play opponent
- Training run produces saved BattleTransformer checkpoint with decreasing validation loss
</remaining_work>

<decisions_made>

- `_ReplayEnv` at module level (not inside `train_browser()`) — needed for direct `from src.ml.browser_trainer import _ReplayEnv` in tests; lazy imports in `__init__` avoid loading gymnasium at collection time
- `DEFAULT_RESULTS_DIR` kept as module-level constant with correct value `"data/ml/results"` — `test_showdown_player.py::TestBrowserTrainerImport::test_constants_defined` imports it; plan's "grep returns 0" acceptance criterion conflicted with existing test; correct path is the fix, not deletion
- `policy.learn()` lambda uses default-arg capture `lambda t=all_transitions: _ReplayEnv(t)` — avoids closure bug where all lambdas see final value of mutable list
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/ROADMAP.md` — Phase 05 goal, requirements (MCTS-01/02/03), success criteria
2. `src/ml/mcts.py` — current MCTS implementation state
3. `src/ml/self_play.py` — MCTSPlayer integration point
4. `src/ml/transformer_model.py` — BattleTransformer architecture

## Critical Anti-Patterns (do NOT repeat these)

- **plan acceptance criteria vs existing tests**: Plan 04-01 said `grep -c "DEFAULT_RESULTS_DIR = "` should return 0, but an existing test required the export. Always run the existing test suite before calling a plan's acceptance criteria complete — tests trump plan expectations.

## Infrastructure State

- All changes committed to `master` (HEAD: `b5025f0`)
- No running services
- `.venv` present and functional; playwright NOT installed in venv (only declared in requirements.txt)
  - Run `pip install playwright && playwright install chromium` before actual browser training

<context>
Phase 04 fully complete this session. 04-01 made targeted edits to 3 files; one regression
(existing test expecting module-level DEFAULT_RESULTS_DIR export) caught and fixed immediately.
04-02 wrote 14 tests; all green first run. Phase 05 is the natural next step.
</context>

<next_action>
Start with: /gsd:plan-phase 5
</next_action>
