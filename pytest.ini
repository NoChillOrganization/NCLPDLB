[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --cov --cov-report=term-missing --timeout=60
markers =
    integration: end-to-end tests requiring a live local Showdown server
filterwarnings =
    ignore::DeprecationWarning:discord.player
